# Data Folder

Place the workshop dataset here as:

- `data.csv`

Expected notebook path:

- `data/data.csv`

Do not place `model.json`, `inference_test.csv`, or other workshop artifacts here for modeling.
